# Database (required for auth, interviews, resume, analytics)
DATABASE_URL="postgresql://user:password@localhost:5432/ai_interview_coach"

# Firebase Admin (required for real auth; verify Google/LinkedIn id tokens)
# Get from Firebase Console → Project settings → Service accounts → Generate new key
FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"...",...}'

# OpenAI (required for AI interview, feedback, resume, job match, behavioral)
OPENAI_API_KEY=sk-...

# Stripe (optional; for Premium subscriptions)
STRIPE_SECRET_KEY=sk_...
STRIPE_PREMIUM_PRICE_ID=price_...
STRIPE_WEBHOOK_SECRET=whsec_...

PORT=3001
